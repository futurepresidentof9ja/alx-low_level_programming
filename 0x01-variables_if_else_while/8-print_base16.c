#include <stdio.h>
#include <stdlib.h>

/**
 *  *  * main - Entry point
 *   *   *
 *    *    * Return: Always 0 (Success)
 *     **/

int main(void)

{
	int d;

for (d = '0'; d <= '9'; d++)
putchar(d);
for (low = 'a'; low <= 'f'; low++)
putchar(low);
puthcar('\n');

return (0);
}
